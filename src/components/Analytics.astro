---
interface Props {
  analyticsId: string
}

const { analyticsId } = Astro.props

const isProd = import.meta.env.PROD
---
{isProd && (
  <script defer src="https://analytics.ulubit.com/app.js" data-website-id={analyticsId}></script>
)}